---
const { href = '/', ...props } = Astro.props;

const base = import.meta.env.BASE_URL || '/';

const finalHref = /^https?:\/\//.test(href)
  ? href
  : `${base}${href.replace(/^\//, '')}`;
---

<a href={finalHref} {...props}>
  <slot />
</a>

