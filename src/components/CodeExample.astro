---
---

<section id="code" class="py-16 sm:py-20 md:py-28">
  <div class="mx-auto max-w-6xl w-full px-4 sm:px-6 lg:px-8 xl:px-8">
    <div class="grid lg:grid-cols-2 gap-8 md:gap-10 lg:gap-12 items-center">
      <div class="min-w-0 order-2 lg:order-1">
        <h2 class="text-3xl font-bold tracking-tight text-zinc-900 dark:text-white sm:text-4xl">
          Integrate in minutes
        </h2>
        <p class="mt-3 sm:mt-4 text-base sm:text-lg text-zinc-600 dark:text-zinc-400">
          Use the Python SDK to evaluate flags in your app. Type-safe, with optional local caching and async support.
        </p>
        <ul class="mt-4 sm:mt-6 space-y-2 sm:space-y-3 text-sm sm:text-base text-zinc-600 dark:text-zinc-400">
          <li class="flex items-center gap-2 flex-wrap"><span class="text-brand-500">✓</span> Install with <code class="rounded bg-zinc-100 dark:bg-zinc-800 px-1.5 py-0.5 font-mono text-xs sm:text-sm break-all">pip install flagswitch</code></li>
          <li class="flex items-center gap-2"><span class="text-brand-500">✓</span> Evaluate flags by user, segment, or percentage</li>
          <li class="flex items-center gap-2"><span class="text-brand-500">✓</span> Works with FastAPI, Django, Flask, and more</li>
        </ul>
      </div>
      <div class="relative min-w-0 order-1 lg:order-2">
        <div class="rounded-xl sm:rounded-2xl border border-zinc-200 dark:border-zinc-800 bg-zinc-900 dark:bg-zinc-950 overflow-hidden shadow-xl">
          <div class="flex items-center gap-2 border-b border-zinc-700 px-3 sm:px-4 py-2.5 sm:py-3">
            <span class="h-2.5 w-2.5 sm:h-3 sm:w-3 rounded-full bg-red-500/80"></span>
            <span class="h-2.5 w-2.5 sm:h-3 sm:w-3 rounded-full bg-amber-500/80"></span>
            <span class="h-2.5 w-2.5 sm:h-3 sm:w-3 rounded-full bg-emerald-500/80"></span>
            <span class="ml-2 text-xs font-medium text-zinc-500">main.py</span>
          </div>
          <pre class="p-3 sm:p-4 md:p-6 overflow-x-auto text-xs sm:text-sm max-w-full"><code class="font-mono text-zinc-300 block min-w-max"><span class="text-purple-400">from</span> flagswitch <span class="text-purple-400">import</span> FlagSwitch

client = FlagSwitch(api_key=<span class="text-amber-300">"your_api_key"</span>)

<span class="text-zinc-500"># Check if a feature is enabled</span>
<span class="text-purple-400">if</span> client.is_enabled(<span class="text-amber-300">"new_checkout"</span>, user_id=<span class="text-amber-300">"user_123"</span>):
    render_new_checkout()
<span class="text-purple-400">else</span>:
    render_legacy_checkout()

<span class="text-zinc-500"># Percentage rollout (e.g. 20% of users)</span>
<span class="text-purple-400">if</span> client.is_in_rollout(<span class="text-amber-300">"beta_dashboard"</span>, user_id=<span class="text-amber-300">"user_123"</span>, percentage=<span class="text-emerald-400">20</span>):
    show_beta_dashboard()

<span class="text-zinc-500"># Environment-aware (dev / staging / prod)</span>
client.set_environment(<span class="text-amber-300">"production"</span>)</code></pre>
        </div>
      </div>
    </div>
  </div>
</section>
